npm run electron:dev

- 이 프로젝트의 이름과 개발되어야 할 프로그램의 이름은 `BookManager`입니다.
- 현재 Python으로 개발된 `/old_project/ComicZIP Optimizer` 애플리케이션을 Electron+ Node.js + React 기반으로 마이그레이션해야 합니다.
- 최종 목표
   1. 기존 애플리케이션의 모든 기능 100% 동일하게 구현.
   2. UI 레이아웃, 버튼 위치, 메뉴 구조, 텍스트 문구, 아이콘을 절대 변경하지 않고 그대로 유지.
   3. 이미지(`src/`), 사운드(`sounds/`), 폰트(`fonts/`) 리소스를 100% 재사용.
   4. 윈도우(Windows) 및 맥(macOS) 크로스플랫폼 완벽 지원.(윈도우, 맥에서 실행 가능한 포터블 프로그램으로 빌드)
   5. 사용자 입장에서 디자인을 제외한 기존 프로그램과 작동 방식이나 기능적 차이를 전혀 느낄 수 없도록 구성.
- 기존 파이썬 파일중 몇몇 파일은 용량이 많고 라인수가 길어서 참조시 문제되지 않도록해야한다.
- 기존 파이썬 파일중 용량이 큰 파일은 한번에 마이그레이션 하지않고, 분해하여 모듈화 시켜야한다
- 마이그레이션될 새로운 폴더와 파일만 먼저 생성하고 그 이후에 코드를 작성하면 좋을것 같다.
- /MIGRATION_TRACKING.md 파일에 작업할 내역을 디테일하게 리스트업하고 작업시 과정을 체크한다.
- 마이그레이션중 니가 과부화되지 않게 하기위해 최대한 최적화된 작업 방식을 선택해줘
- `/old_projoect/ui_screenshot` 폴더에 기존 파이썬 프로그램의 화면을 스샷을 찍어놨다. 동일한 화면 구성으로 개발되어야한다.

- 프로젝트 진행의 큰 흐름은 아래와 같다.
   1. 프로젝트 셋업 및 기본 설정
   2. UI 레이아웃 및 스타일 포팅
   3. 설정 및 다국어(i18n) 마이그레이션
   4. 메인 비즈니스 로직 및 백그라운드 워커
   5. 외부 바이너리 실행 로직 변환
   6. 로컬 서버 포팅