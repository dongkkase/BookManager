# BookManager Python → Electron/React 상세 마이그레이션 체크리스트

> 작성일: 2026-06-18  
> 원본: `old_project/ComicZIP_Optimizer` (PyQt6)  
> 대상: 현재 `BookManager` Electron + Node.js + React 프로젝트  
> 목표: 사용자가 UI, 입력, 선택, 알림, 파일 처리 결과에서 차이를 느끼지 못하는 수준의 기능 동등성

## 0. 체크 기준과 사용법

### 상태 표기

- [ ] 미검증 또는 미완료
- [-] 원본과 동일하게 구현하고 직접 검증 완료
- 작업 중인 항목은 체크박스를 완료 처리하지 말고 바로 아래에 `진행 메모:`를 남긴다.
- “코드가 존재함”은 완료가 아니다. 정상·실패·취소·빈 값·중복 입력까지 실제 동작을 검증해야 완료한다.

### 완료 판정 규칙

- [ ] 원본 Python 화면과 Electron 화면을 같은 해상도에서 나란히 비교했다.
- [ ] 버튼 문구, 아이콘, 순서, 간격, 활성/비활성 상태가 동일하다.
- [ ] 클릭, 더블 클릭, 우클릭, 드래그, 키보드 입력, 단축키 동작이 동일하다.
- [ ] 입력값의 기본값, 허용 범위, placeholder, 마스킹, 자동 보정이 동일하다.
- [ ] 드롭다운의 항목, 순서, 기본 선택, 변경 시 부수 효과가 동일하다.
- [ ] 확인/경고/오류/정보 팝업의 발생 조건, 버튼 구성, 기본 버튼, 취소 결과가 동일하다.
- [ ] Toast의 발생 조건, 문구, 표시 위치, 중복 표시 방지가 동일하다.
- [ ] 작업 중 잠금, 진행률, 취소, 완료음, 완료 로그가 동일하다.
- [ ] 파일명, 출력 경로, 백업, 덮어쓰기, 중복 충돌 결과가 동일하다.
- [ ] 설정 저장 후 즉시 반영 여부와 재시작 필요 여부가 동일하다.
- [ ] Windows와 macOS에서 각각 검증했다.

### 원본 대조 자료

- Python UI: `old_project/ComicZIP_Optimizer/ui/`
- Python 작업 로직: `old_project/ComicZIP_Optimizer/tasks/`
- Python 핵심 로직: `old_project/ComicZIP_Optimizer/core/`
- Python 서버: `old_project/ComicZIP_Optimizer/servers/`
- 기준 스크린샷: `old_project/ui_screenshot/`
- 현재 React UI: `src/`
- 현재 Electron/Node 로직: `electron/`

---

## 1. 공통 애플리케이션 셸

원본 근거: `ui/main_window.py`, `main.py`, `config.py`  
현재 대응: `src/App.jsx`, `src/components/TabBar.jsx`, `electron/main.js`, `electron/configManager.js`

### 1.1 창 생성 및 상태 복원

- [-] 앱 이름과 창 제목이 `BookManager` 정책에 맞으면서 원본 표시 규칙을 보존한다.
- [ ] 앱 아이콘이 창, 작업 표시줄, Dock, 빌드 결과물에서 정상 표시된다.
- [ ] 최초 창 크기와 최소 크기가 원본과 동일하다.
- [ ] 종료 시 일반 창의 width/height를 저장한다.
  - 진행 메모: Electron 종료 시 `getNormalBounds()`의 좌표와 크기를 config에 저장하도록 구현했다. GUI 재실행 검증 필요.
- [ ] 최대화 여부를 저장하고 다음 실행 시 복원한다.
  - 진행 메모: `is_maximized` 저장 및 `ready-to-show` 시 복원을 구현했다. macOS/Windows 재실행 검증 필요.
- [ ] 마지막으로 선택한 탭을 저장하고 다음 실행 시 복원한다.
  - 진행 메모: 탭 변경 즉시 `last_tab_index`를 저장하고 config 로드 후 복원하도록 구현했다. 재실행 검증 필요.
- [ ] 창이 화면 밖 좌표에 저장된 경우 현재 모니터 안으로 복구한다.
  - 진행 메모: 저장 좌표가 연결된 모든 디스플레이의 work area와 겹치지 않으면 주 모니터 중앙으로 복구하도록 구현했다. 모니터 분리 시나리오 검증 필요.
- [ ] 고해상도 배율에서 글자와 아이콘이 흐려지거나 잘리지 않는다.
- [ ] macOS traffic light 영역과 Windows title bar에서 주요 버튼이 가려지지 않는다.

### 1.2 상단 공통 툴바

- [ ] `폴더 추가` 버튼의 아이콘, 문구, hover, 클릭 영역이 동일하다.
- [ ] `파일 추가` 버튼의 아이콘, 문구, hover, 클릭 영역이 동일하다.
- [ ] `선택 삭제` 버튼은 현재 탭의 체크된 항목만 제거한다.
- [ ] `전체 비우기` 버튼은 현재 탭 데이터만 비운다.
- [ ] `전체 선택/해제` 버튼은 현재 탭 전체 항목의 체크 상태를 토글한다.
- [ ] 전체 선택 상태에 따라 체크 아이콘이 채움/빈 사각형으로 전환된다.
- [ ] 폴더 탭에서도 공통 툴바 동작이 원본의 선택 모델과 충돌하지 않는다.
- [-] 공유 서버/릴리즈 탭에서는 의미 없는 툴바 버튼이 원본처럼 숨김 또는 비활성화된다.
- [-] 버그 신고 버튼이 원본 URL을 기본 브라우저로 연다.
- [-] 버전 버튼에 현재 버전이 표시된다.
- [ ] 새 버전 존재 시 버전 버튼의 색상과 문구가 업데이트 상태로 바뀐다.
- [ ] 버전 버튼 클릭 시 업데이트 URL 또는 원본과 동일한 흐름이 실행된다.
- [ ] 설정 버튼이 환경설정 모달을 연다.

### 1.3 탭

- [-] 탭 순서가 `폴더 → 압축 구조 정리 → 내부 파일명 변경 → 메타데이터 관리 → 공유 서버 → 업데이트/릴리즈 노트`이다.
- [ ] 탭 문구가 한국어/영어/일본어에서 원본 i18n과 동일하다.
- [ ] 탭 변경 시 현재 탭에 맞는 하단 실행 버튼 표시 여부가 동일하다.
- [ ] 탭 변경 시 상태 문구가 잘못 남지 않는다.
- [ ] 작업 중에는 탭 변경과 위험한 입력이 원본처럼 잠긴다.
- [ ] 탭 간 자동 전달 시 대상 탭으로 이동하고 전달한 파일이 자동 로드된다.

### 1.4 하단 상태 및 실행 영역

- [ ] 서버 실행 중일 때 서버 상태 아이콘이 표시된다.
- [ ] 서버 상태 아이콘 tooltip에 실행 중인 프로토콜과 URL이 표시된다.
- [ ] 대기 상태 문구가 원본과 동일하다.
- [ ] 로딩 시 상태 문구와 진행률 바가 표시된다.
- [ ] 진행률 바의 높이, 폭, 색상, determinate/indeterminate 상태가 동일하다.
- [ ] 실행 버튼은 원본과 같은 탭에서만 표시 또는 활성화된다.
- [ ] 실행 시작 시 버튼이 `취소`와 정지 아이콘으로 바뀐다.
- [ ] 취소 클릭 후 `취소 대기` 문구로 바뀌고 중복 클릭이 막힌다.
- [ ] 작업 종료 후 버튼, 상태, 진행률이 반드시 원상 복구된다.
- [ ] 예외가 발생해도 dim overlay와 입력 잠금이 남지 않는다.

### 1.5 전역 드래그 앤 드롭

- [-] 지원 파일과 폴더를 창 어디에 드롭해도 현재 탭으로 전달된다.
- [-] 공유 서버와 릴리즈 탭에서는 드롭을 무시한다.
- [-] 작업 중 드롭은 무시한다.
- [-] 여러 파일과 폴더를 함께 드롭해도 순서와 중복 제거 방식이 원본과 같다.
- [ ] 지원하지 않는 확장자는 스킵되고 로그 또는 알림에 기록된다.
- [ ] 메타데이터 탭에 개별 파일을 드롭하면 “상위 폴더 전체 추가” 여부를 `예/아니오/취소`로 묻는다.
- [ ] 위 질문에서 `예`는 각 파일의 부모 폴더를 추가한다.
- [ ] `아니오`는 선택 파일만 추가한다.
- [ ] `취소`는 아무것도 추가하지 않는다.

### 1.6 종료 처리

- [ ] 작업이 없을 때 즉시 정상 종료한다.
- [ ] 구조 정리/이름 변경/메타 저장 중 종료하면 위험 경고가 표시된다.
- [ ] 종료 경고의 기본 선택이 `아니오`이다.
- [ ] `아니오` 선택 시 종료하지 않고 작업을 유지한다.
- [ ] `예` 선택 시 취소 신호를 보내고 안전하게 프로세스를 종료한다.
- [ ] 종료 시 창 크기, 최대화, 마지막 탭, 폴더 탭 splitter 상태를 저장한다.

---

## 2. 공통 파일 선택, 메시지, Toast, 로그

### 2.1 파일/폴더 선택기

- [-] 폴더 선택기는 디렉터리만 선택한다.
- [-] 파일 선택기는 `zip, cbz, cbr, 7z, rar`를 표시한다.
- [-] 다중 파일 선택이 가능하다.
- [-] 취소 시 목록과 상태가 변하지 않는다.
- [ ] macOS와 Windows의 경로 구분자를 내부에서 안전하게 정규화한다.
- [ ] 한글, 일본어, 이모지, 조합형 유니코드 경로를 처리한다.
- [ ] 네트워크 드라이브/NAS 경로를 처리한다.

### 2.2 메시지 유형

- [ ] 정보 팝업은 파괴적 동작에 사용되지 않는다.
- [ ] 경고 팝업은 입력 누락, 대상 없음, 외부 프로그램 누락에 사용된다.
- [ ] 오류 팝업은 파일 시스템/권한/압축 처리 실패에 사용된다.
- [ ] 질문 팝업은 `예/아니오`, `예/아니오/취소` 구성을 정확히 구분한다.
- [ ] 모달이 열린 동안 배경의 단축키가 실행되지 않는다.
- [ ] Enter/Escape가 원본의 확인/취소 버튼과 동일하게 동작한다.
- [ ] 팝업 문구의 줄바꿈과 동적 값 삽입이 원본과 같다.

### 2.3 Toast

- [ ] 성공, 캐시 삭제, 이름 변경, 자동 입력, 누락 권수 알림에 Toast를 사용한다.
- [-] Toast가 메인 창 기준 위치에 표시된다.
- [-] Toast가 클릭을 가로막지 않는다.
- [-] 동일 알림이 짧은 시간에 무한 중복되지 않는다.
- [ ] 다국어 변경 후 새 언어 문구로 표시된다.
- [-] 긴 문구가 잘리지 않고 적절히 줄바꿈된다.

### 2.4 작업 결과 로그 다이얼로그

- [ ] 제목, 크기, 어두운 테마가 원본 `LogDialog`와 동일하다.
- [ ] 상단에 Success/Skip/Error 개수를 표시한다.
- [ ] 본문에 `[ERRORS]`, `[SUCCESS]`, `[SKIPPED]` 순으로 내용을 표시한다.
- [ ] 로그 본문은 읽기 전용이며 스크롤/복사가 가능하다.
- [ ] `닫기` 버튼이 다이얼로그를 닫는다.
- [ ] 구조 정리 완료 후 결과 파일이 있으면 `내부 파일명 변경으로 계속` 버튼을 표시한다.
- [ ] 이름 변경 완료 후 결과 파일이 있으면 `메타데이터 관리로 계속` 버튼을 표시한다.
- [ ] 계속 버튼을 누르면 대상 탭 이동과 파일 자동 로드가 한 번만 실행된다.
- [ ] 취소된 작업의 로그에는 계속 버튼을 표시하지 않는다.

---

## 3. 폴더 탭

원본 근거: `ui/tabs/tab_folder.py`, `tab_folder_models.py`, `tab_folder_threads.py`, `tab_folder_ui.py`  
현재 대응: `src/tabs/FolderTab.jsx`, `src/components/folder/`, `src/hooks/useFolderScan.js`, `electron/tasks/folderScanTask.js`

기준 스크린샷: `old_project/ui_screenshot/1. 폴더 메뉴/1.png` ~ `8.png`

### 3.1 전체 레이아웃과 splitter

- [ ] 좌측 탐색 패널과 우측 콘텐츠 패널의 초기 폭 비율이 동일하다.
- [ ] 좌우 splitter를 드래그해 폭을 조절할 수 있다.
- [ ] 우측 파일 목록과 하단 상세 패널 사이 높이를 조절할 수 있다.
- [ ] splitter hover 색상과 dashed handle 표현이 동일하다.
- [ ] splitter 위치를 종료 시 저장하고 다음 실행 시 복원한다.
- [ ] 사이드바 숨김 시 목록 영역이 즉시 확장된다.
- [ ] 사이드바 재표시 시 이전 폭을 복원한다.

### 3.2 좌측 상단 토글

- [ ] `하위 폴더 포함`은 checkable 버튼이다.
- [ ] OFF/ON에 따라 버튼 문구, 배경색, 테두리, 글자 굵기가 바뀐다.
- [ ] 하위 폴더 포함 변경 시 현재 폴더를 다시 스캔한다.
- [ ] `중복 검사`는 checkable 버튼이다.
- [ ] 중복 검사 ON 시 인덱스/캐시 준비 후 매칭을 시작한다.
- [ ] 진행 중에는 진행 상태가 보이고 중복 실행되지 않는다.
- [ ] OFF 시 중복 표시와 필터 결과를 원본처럼 해제한다.
- [ ] `폴더 새로고침`은 트리 루트와 빠른 이동 목록을 갱신한다.

### 3.3 라이브러리 목록

- [ ] 환경설정의 `dup_check_folders`가 라이브러리 목록에 표시된다.
- [ ] 라이브러리 항목은 전체 경로를 tooltip으로 제공한다.
- [ ] 항목 클릭 시 해당 폴더로 트리 이동 후 목록을 로드한다.
- [ ] 라이브러리 제목 옆 톱니 버튼이 환경설정의 폴더 탭 설정을 연다.
- [ ] 라이브러리 우클릭 메뉴에 `인덱스 동기화`가 있다.
- [ ] 라이브러리 우클릭 메뉴에 `메타데이터 최적화`가 있다.
- [ ] 라이브러리 우클릭 메뉴에 `탐색기에서 열기`가 있다.
- [ ] 인덱스 동기화 선택 시 스마트 업데이트/강제 재스캔/취소 선택 모달이 뜬다.
- [ ] 마지막으로 선택한 라이브러리가 필요한 다이얼로그에서 복원된다.

### 3.4 즐겨찾기와 빠른 위치

- [ ] 즐겨찾기가 config에서 로드된다.
- [ ] 즐겨찾기 클릭 시 폴더 트리와 목록이 동기화된다.
- [ ] 선택 폴더를 즐겨찾기에 추가할 수 있다.
- [ ] 같은 경로를 중복 추가하지 않는다.
- [ ] 즐겨찾기를 제거할 수 있다.
- [ ] 데스크톱, 문서, 다운로드, 홈 빠른 이동 아이콘이 표시된다.
- [ ] 각 아이콘 tooltip이 현 언어에 맞다.
- [ ] 빠른 이동 시 라이브러리/즐겨찾기 선택을 해제한다.
- [ ] 경로가 존재하지 않으면 조용히 무시하거나 원본과 같은 알림을 낸다.

### 3.5 폴더 트리

- [ ] 드라이브와 폴더를 lazy loading한다.
- [ ] 파일은 표시하지 않고 폴더만 표시한다.
- [ ] 확장/축소 애니메이션과 indentation이 동일하다.
- [ ] 긴 경로는 말줄임 없이 가로 스크롤로 볼 수 있다.
- [ ] 선택, hover, branch 영역 색상이 원본과 같다.
- [ ] 폴더 선택 시 우측 목록을 갱신한다.
- [ ] 폴더명 변경 후 현재 선택과 스크롤 위치를 유지한다.
- [ ] 폴더 삭제 후 인접 폴더 또는 부모 폴더를 안정적으로 선택한다.
- [ ] 파일 시스템 변경 감지 또는 NAS mtime polling으로 외부 변경을 갱신한다.
- [ ] 앱 내부 파일 이동/삭제 중에는 watcher가 중복 새로고침하지 않는다.

### 3.6 폴더 트리 우클릭 메뉴

- [ ] 메뉴가 클릭 위치 근처에 표시되고 화면 밖으로 잘리지 않는다.
- [ ] `새로고침`이 선택 폴더를 갱신한다.
- [ ] `이름 바꾸기`가 현재 이름을 기본값으로 넣은 input을 연다.
- [ ] 빈 이름 입력은 변경하지 않는다.
- [ ] 동일 이름/중복 경로는 오류를 표시한다.
- [ ] `삭제`는 확인 팝업 후 폴더를 삭제한다.
- [ ] 삭제 확인에서 `아니오`는 아무것도 변경하지 않는다.
- [ ] 삭제 실패 시 오류 원인을 표시한다.
- [ ] `즐겨찾기에 추가/제거`가 현재 상태에 따라 바뀐다.
- [ ] `탐색기에서 열기`가 Windows Explorer/macOS Finder에서 연다.
- [ ] `이 폴더로 이동` 또는 라이브러리 이동 기능이 원본 메뉴 조건에 맞게 표시된다.
- [ ] `압축 구조 정리로 보내기`, `내부 파일명 변경으로 보내기`, `메타데이터 관리로 보내기`가 있다.

### 3.7 우측 목록 툴바

- [ ] `사이드바` 버튼이 ON/OFF 문구와 상태를 표시한다.
- [ ] `그룹` 드롭다운에 없음, 폴더, 확장자, 시리즈 등 원본 항목이 순서대로 있다.
- [ ] 현재 그룹 기준이 버튼 활성 스타일로 표시된다.
- [ ] `필터` 드롭다운에 `메타데이터 없음` checkable 항목이 있다.
- [ ] 필터 ON/OFF 시 목록과 버튼 스타일이 즉시 갱신된다.
- [ ] `정렬` 드롭다운에 이름, 크기, 수정일, 확장자, 시리즈, 제목, 작가가 있다.
- [ ] 정렬 오름/내림차순 전환 항목이 있다.
- [ ] 표 헤더 클릭 정렬과 정렬 드롭다운 상태가 동기화된다.
- [ ] `레이아웃` 드롭다운에 편집, 현재 레이아웃 저장, 저장 레이아웃 삭제가 있다.
- [ ] 저장된 사용자 레이아웃이 메뉴 하단에 동적으로 표시된다.
- [ ] `CSV 내보내기` 버튼이 있다.
- [ ] 검색창 placeholder와 clear 버튼이 원본과 같다.
- [ ] 검색은 파일명, 경로, 시리즈, 제목 등 원본 필드를 대상으로 한다.
- [ ] 검색 중 선택/스크롤 상태가 비정상적으로 초기화되지 않는다.
- [ ] `목록 새로고침`은 스마트 업데이트/강제 갱신 규칙을 따른다.

### 3.8 레이아웃 편집과 저장

- [ ] 레이아웃 편집 다이얼로그에 모든 원본 컬럼이 표시된다.
- [ ] 커버, 파일명, 용량, 해상도, 수정일, 생성일, 시리즈, 제목, 권, 화, 작가, 시리즈 그룹, 제작진, 출판사 항목을 켜고 끌 수 있다.
- [ ] 컬럼 순서를 변경할 수 있다.
- [ ] 확인 시 즉시 표 헤더에 반영된다.
- [ ] 취소 시 변경 전 상태를 유지한다.
- [ ] 현재 컬럼 순서와 폭을 이름을 입력해 저장할 수 있다.
- [ ] 빈 레이아웃 이름은 저장하지 않는다.
- [ ] 같은 이름 처리 방식이 원본과 같다.
- [ ] 저장된 레이아웃을 드롭다운에서 적용할 수 있다.
- [ ] 삭제할 레이아웃을 드롭다운 input으로 선택할 수 있다.
- [ ] 삭제 후 config와 메뉴가 즉시 갱신된다.

### 3.9 보기 모드

- [ ] 상세 목록 보기 버튼과 tooltip이 있다.
- [ ] 썸네일 보기 버튼과 tooltip이 있다.
- [ ] 타일 보기 버튼과 tooltip이 있다.
- [ ] 현재 보기 모드 버튼만 active 상태다.
- [ ] 보기 모드 전환 시 선택 항목을 유지한다.
- [ ] 보기 모드 전환 시 스크롤 위치를 가능한 범위에서 유지한다.
- [ ] 크기 slider가 보기 모드별 아이템 크기를 조절한다.
- [ ] 보기 모드별 마지막 크기를 config에 저장한다.
- [ ] 커버가 없는 항목은 원본 placeholder를 표시한다.
- [ ] 썸네일 로딩 중 placeholder/스피너 동작이 동일하다.
- [ ] 그룹 헤더와 항목 정렬이 모든 보기 모드에서 동일하다.

### 3.10 파일 선택

- [ ] 단일 클릭은 한 항목만 선택한다.
- [ ] Ctrl/Cmd 클릭은 개별 선택을 추가/제거한다.
- [ ] Shift 클릭은 범위를 선택한다.
- [ ] 빈 영역 클릭 시 원본과 동일하게 선택을 해제한다.
- [ ] `Ctrl/Cmd+A`로 전체 파일을 선택한다.
- [ ] 선택 반전 단축키/메뉴가 원본과 같다.
- [ ] 선택 파일 수와 전체 용량이 상태 영역에 반영된다.
- [ ] 선택 항목 변경 시 하단 상세 패널이 마지막 활성 항목 기준으로 갱신된다.
- [ ] 키보드 위/아래 이동 시 상세 패널도 갱신된다.

### 3.11 파일 목록 우클릭 메뉴

- [ ] `뷰어로 열기`가 설정한 외부 뷰어를 실행한다.
- [ ] 뷰어 경로 미설정 시 경고를 표시한다.
- [ ] `탐색기에서 열기`가 선택 파일을 Finder/Explorer에서 선택 상태로 연다.
- [ ] `이름 바꾸기(F2)`가 input을 연다.
- [ ] 확장자를 실수로 제거하거나 바꿀 때 원본 보호 규칙을 따른다.
- [ ] 같은 이름이 존재하면 경고하고 변경하지 않는다.
- [ ] 권한 오류와 일반 오류를 구분한다.
- [ ] 이름 변경 성공 시 Toast와 목록/메타 캐시를 갱신한다.
- [ ] `마지막 이름 변경 실행 취소`가 rename history를 사용한다.
- [ ] 실행 취소할 기록이 없으면 경고한다.
- [ ] `여러 파일 이름 바꾸기`가 전용 다이얼로그를 연다.
- [ ] `삭제`는 확인 후 실제 파일을 삭제한다.
- [ ] 여러 선택 삭제가 모든 선택 파일에 적용된다.
- [ ] `시리즈별 폴더로 정리`가 이동 미리보기 다이얼로그를 연다.
- [ ] `라이브러리로 이동`이 라이브러리 선택 다이얼로그를 연다.
- [ ] `압축 구조 정리로 보내기`, `내부 파일명 변경으로 보내기`, `메타데이터 관리로 보내기`가 있다.

### 3.12 여러 파일 이름 바꾸기 다이얼로그

원본 근거: `ui/dialogs.py::MultiRenameDialog`

- [ ] 다이얼로그 크기는 1000×650을 기준으로 한다.
- [ ] 기존 형식과 새 형식 IME 입력창이 있다.
- [ ] 파일 목록에서 공통/가변 토큰을 분석해 `%1`, `%2` 패턴을 자동 추론한다.
- [ ] `대소문자 구분` 체크가 미리보기에 반영된다.
- [ ] `정규식 모드` 체크 시 일반 패턴을 `(.*)`, `\1` 형식으로 변환한다.
- [ ] 정규식 해제 시 `%1` 형식으로 복원한다.
- [ ] `폴더명으로 이름 바꾸기`가 부모 폴더명을 새 형식에 삽입한다.
- [ ] 체크 해제 시 이전 새 형식 입력값을 복원한다.
- [ ] `숫자 자리수 맞추기` 체크 시 자리수 spinbox가 활성화된다.
- [ ] 자리수 범위는 1~4, 기본값은 3이다.
- [ ] `순번 추가` 체크 시 시작/자리수/위치 입력이 활성화된다.
- [ ] 시작 범위는 0~99999, 기본값은 1이다.
- [ ] 순번 자리수 범위는 1~10, 기본값은 3이다.
- [ ] 위치 드롭다운은 앞/뒤 순서다.
- [ ] 입력 변경 후 100ms debounce로 미리보기를 갱신한다.
- [ ] 이전 preview worker를 취소하고 최신 결과만 표시한다.
- [ ] 미리보기 컬럼은 이전 파일명/새 파일명/상태/경로다.
- [ ] 정상 행만 실제 rename task에 포함한다.
- [ ] 이름이 변하지 않는 행은 실행 대상에서 제외한다.
- [ ] 실행 중 확인 버튼을 비활성화하고 진행률을 표시한다.
- [ ] 성공 이력을 `rename_history.json`에 저장하고 최근 10개만 유지한다.
- [ ] 일부 실패 시 성공 개수와 최대 10개 오류를 경고 팝업으로 표시한다.
- [ ] 전체 성공 시 성공 개수 Toast를 표시한다.

### 3.13 상세 정보 패널

- [ ] 선택 파일 커버를 배경 blur/gradient와 함께 표시한다.
- [ ] 파일명, 전체 경로, 크기, 확장자, 해상도, 날짜를 표시한다.
- [ ] ComicInfo의 시리즈, 제목, 권, 화, 작가, 제작진, 출판사, 장르, 태그를 표시한다.
- [ ] 값이 없는 필드는 원본과 같은 `-` 또는 빈 값 처리다.
- [ ] 태그 chip 제거/표시 동작이 원본과 같다.
- [ ] 중복 파일이 있으면 중복 상태와 대상 경로를 표시한다.
- [ ] 패널 높이가 작아도 스크롤 또는 축약으로 내용이 겹치지 않는다.

### 3.14 누락 권수 검사

- [ ] 앱 진입 후 백그라운드에서 누락 권수를 계산한다.
- [ ] 최초 발견 시 한 번만 전역 Toast를 표시한다.
- [ ] 이후 선택 폴더 기준 누락 알림은 지연/중복 방지 규칙을 따른다.
- [ ] 버튼에 누락 시리즈 수 badge가 표시된다.
- [ ] 분석 중 클릭하면 버튼을 비활성화하고 `분석 중` 문구를 표시한다.
- [ ] 결과가 없으면 정보 팝업을 표시한다.
- [ ] 결과가 있으면 550×450 기준 다이얼로그를 연다.
- [ ] 각 행에 시리즈명, 누락 권수, 이동 버튼을 표시한다.
- [ ] 누락이 8개를 넘으면 앞 8개와 전체 개수 요약을 표시한다.
- [ ] 이동 버튼 클릭 시 다이얼로그를 닫고 해당 폴더로 이동한다.
- [ ] 이동 후 그룹 기준을 폴더로 바꾼다.

### 3.15 CSV 내보내기

- [ ] 데이터가 없으면 `내보낼 데이터 없음` 정보 팝업을 표시한다.
- [ ] 저장 파일 선택기의 기본 확장자와 필터가 CSV다.
- [ ] 취소하면 아무 파일도 생성하지 않는다.
- [ ] 한글이 Excel에서 깨지지 않는 인코딩으로 저장된다.
- [ ] 표시 컬럼과 원본 내보내기 컬럼/순서가 동일하다.
- [ ] 성공 시 완료 팝업을 표시한다.
- [ ] 실패 시 오류 상세를 표시한다.

### 3.16 라이브러리 이동과 충돌 해결

- [ ] 라이브러리가 없으면 경고를 표시한다.
- [ ] 라이브러리 선택 드롭다운에 등록 폴더가 순서대로 표시된다.
- [ ] 마지막 선택 라이브러리를 기본값으로 복원한다.
- [ ] 파일 이동 시 `현재 폴더 이름으로 폴더 생성` 체크가 기본 ON이다.
- [ ] 체크 변경 시 Source/Destination 미리보기가 즉시 갱신된다.
- [ ] 폴더 자체 이동 모드에서는 위 체크를 숨긴다.
- [ ] 확인 시 미리보기 경로 그대로 이동한다.
- [ ] 취소 시 파일을 이동하지 않는다.
- [ ] 대상에 같은 이름이 있으면 충돌 해결 다이얼로그를 연다.
- [ ] 충돌 다이얼로그에 원본/대상 커버, 크기, 해상도를 비교 표시한다.
- [ ] `덮어쓰기`, `새 이름으로 저장`, `건너뛰기` 세 선택이 있다.
- [ ] 각 선택 결과가 이후 파일에도 잘못 재사용되지 않는다.
- [ ] 이동 완료 후 빈 원본 폴더를 안전하게 제거한다.
- [ ] 이동 후 목록, 선택, 인덱스, 썸네일 캐시를 갱신한다.

---

## 4. 압축 파일 구조 정리(평탄화) 탭

원본 근거: `ui/tabs/tab1_organizer.py`, `tasks/organize_task.py`  
현재 대응: `src/tabs/OrganizerTab.jsx`, `electron/tasks/organizerTask.js`, `electron/tasks/organizeTask.js`

기준 스크린샷: `old_project/ui_screenshot/2. 압축 파일 구조 정리(평탄화)/1.png` ~ `5.png`

### 4.1 빈 화면과 입력

- [ ] 목록이 비었을 때 원본 drag-and-drop 이미지 3종 중 하나를 표시한다.
- [ ] 이미지 크기 256×256, opacity 0.55를 재현한다.
- [ ] drag-and-drop 안내 문구가 현재 언어로 표시된다.
- [ ] 공통 파일/폴더 추가와 드롭으로 아카이브를 분석한다.
- [ ] 폴더 추가는 하위 지원 아카이브를 재귀 탐색한다.
- [ ] 이미 추가된 파일은 중복 추가하지 않는다.
- [ ] 손상/미지원 파일은 스킵 목록에 기록한다.

### 4.2 상단 제어

- [ ] `전체 펼치기/접기`가 모든 최상위 항목을 토글한다.
- [ ] `출력 경로 기본값 일괄`은 체크된 항목만 원본 폴더로 설정한다.
- [ ] `책 제목 폴더 일괄`은 체크된 항목만 분석 제목 폴더로 설정한다.
- [ ] 분석 제목이 없으면 `제목없음_수정필요` 폴더를 사용한다.

### 4.3 트리 목록

- [ ] 컬럼은 이름/출력 경로/항목 수/용량 순서다.
- [ ] 항목 수와 용량 컬럼은 우측 정렬이다.
- [ ] 최상위 행에 작업 포함 체크박스가 있다.
- [ ] 원본 파일명과 분석된 clean title을 구분해 표시한다.
- [ ] 최상위 행에 출력 경로 input이 있다.
- [ ] 개별 `기본값`, `책제목`, `일괄: 권`, `일괄: 화` 버튼이 있다.
- [ ] 버튼 클릭 시 해당 파일 데이터만 즉시 갱신한다.
- [ ] 자식 행에 폴더/아카이브 아이콘, 새 파일명, 원본명이 표시된다.
- [ ] 시즌/부/part 폴더 prefix가 보존된다.
- [ ] 외전 항목 표시가 원본과 같다.
- [ ] 대상 포맷 설정에 따라 미리보기 확장자가 바뀐다.
- [ ] 긴 파일명은 줄바꿈되고 행 높이가 자동 증가한다.
- [ ] 목록 갱신 후 확장 상태와 스크롤 위치를 유지한다.
- [ ] 창 크기 변경 시 컬럼 폭 비율을 다시 계산한다.

### 4.4 자식 파일명 편집

- [ ] 자식 행 더블 클릭 시 파일명 편집 팝업을 연다.
- [ ] 자식 행 우클릭 메뉴에 `파일 이름 변경`이 있다.
- [ ] input 기본값은 현재 새 파일명이다.
- [ ] 빈 값은 적용하지 않는다.
- [ ] 확장자 처리와 금지 문자를 원본 규칙대로 보정한다.
- [ ] 변경 즉시 트리 미리보기와 실제 작업 데이터에 반영한다.

### 4.5 선택과 삭제

- [ ] 공통 전체 선택/해제가 최상위 체크 상태를 토글한다.
- [ ] 개별 체크 변경이 내부 데이터에 반영된다.
- [ ] `선택 삭제`는 체크된 최상위 항목을 제거한다.
- [ ] Delete 키는 하이라이트된 최상위/자식의 부모 항목을 제거한다.
- [ ] 삭제 후 인접 행을 선택한다.
- [ ] 전체 비우기 후 빈 화면으로 전환한다.

### 4.6 실행

- [ ] 체크된 대상이 없으면 경고를 표시한다.
- [ ] 실행 전 이름 변경/메타데이터 탭 목록을 원본처럼 비운다.
- [ ] 실행 중 전체 UI를 잠근다.
- [ ] 각 압축의 실제 루트와 이미지 leaf를 분석한다.
- [ ] 다중 권/화 구조를 각각 새 아카이브로 만든다.
- [ ] 출력 경로 input을 존중한다.
- [ ] 출력 포맷 `변경 안 함/ZIP/CBZ/CBR/7z`를 존중한다.
- [ ] WebP 변환, 품질, 폴더 평탄화, 백업 설정을 존중한다.
- [ ] 동일 출력명이 있으면 안전한 고유 이름을 생성한다.
- [ ] 취소 요청을 파일 단위 또는 안전 지점에서 반영한다.
- [ ] 취소 시 원본 파일과 임시 파일이 손상되지 않는다.
- [ ] 임시 디렉터리를 성공/실패/취소 모두에서 정리한다.
- [ ] 완료음 설정이 ON일 때 선택된 완료음을 재생한다.
- [ ] 완료 로그에서 성공/스킵/오류를 구분한다.
- [ ] 계속 버튼으로 생성 파일을 내부 파일명 변경 탭에 전달한다.

---

## 5. 내부 파일명 변경 탭

원본 근거: `ui/tabs/tab2_renamer.py`, `tasks/rename_task.py`, `tasks/load_task.py`  
현재 대응: `src/tabs/RenamerTab.jsx`, `electron/tasks/renamerTask.js`, `electron/tasks/renameTask.js`

기준 스크린샷: `old_project/ui_screenshot/3. 내부 파일명 변경/1.png` ~ `4.png`

### 5.1 레이아웃과 빈 화면

- [ ] 좌측에 커버/내부 이미지 미리보기 패널이 있다.
- [ ] 우측에 패턴 옵션, 아카이브 목록, 내부 파일 목록이 순서대로 있다.
- [ ] 목록이 비면 원본 drag-and-drop 이미지와 문구를 표시한다.
- [ ] 아카이브 선택이 없으면 커버와 내부 미리보기를 초기화한다.

### 5.2 파일명 패턴 입력

- [ ] 패턴 dropdown 항목과 순서가 i18n의 `patterns`와 동일하다.
- [ ] 기본 번호 패턴 결과가 원본과 동일하다.
- [ ] Page 패턴 결과가 원본과 동일하다.
- [ ] Cover 처리 패턴 결과가 원본과 동일하다.
- [ ] 아카이브 파일명 동기화 패턴 결과가 원본과 동일하다.
- [ ] `사용자 지정` 선택 시 custom input을 활성화하고 focus한다.
- [ ] 다른 패턴 선택 시 custom input을 비활성화한다.
- [ ] 시작 번호 input은 정수 0~999999만 허용한다.
- [ ] 시작 번호 기본값을 config에서 복원한다.
- [ ] `-` 버튼은 0 아래로 내려가지 않는다.
- [ ] `+` 버튼은 1씩 증가한다.
- [ ] 시작 번호 변경 시 즉시 config에 저장하고 미리보기를 갱신한다.
- [ ] `내부 파일명 유지` 체크 시 패턴, 시작 번호, +/- 버튼을 비활성화한다.
- [ ] 유지 해제 시 현재 패턴에 맞게 custom input 상태를 복원한다.

### 5.3 아카이브 목록

- [ ] 컬럼은 파일명/이미지 수/용량/용량 최적화/EXIF 제거 순서다.
- [ ] 최상위 파일명 셀에 작업 포함 체크박스가 있다.
- [ ] 개별 용량 최적화 체크가 데이터에 반영된다.
- [ ] 개별 EXIF 제거 체크가 데이터에 반영된다.
- [ ] 상단 `전체 선택` 버튼이 모든 작업 체크를 토글한다.
- [ ] 상단 `이미지 압축 일괄` 버튼이 모든 파일의 최적화 상태를 토글한다.
- [ ] 상단 `EXIF 제거 일괄` 버튼이 모든 파일의 EXIF 상태를 토글한다.
- [ ] 개별 체크 변경 시 일괄 버튼 상태가 실시간 동기화된다.
- [ ] 작업 후 다시 로드된 상태에서도 일괄 버튼이 실제 데이터와 일치한다.
- [ ] 선택 행 변경 시 내부 파일 목록과 커버를 갱신한다.
- [ ] Delete 키는 하이라이트한 아카이브를 제거한다.

### 5.4 아카이브 우클릭 메뉴

- [ ] `경로 찾기`가 Finder/Explorer에서 파일을 선택한다.
- [ ] `파일 이름 변경`이 현재 파일명을 기본값으로 input을 연다.
- [ ] 파일이 없으면 경고한다.
- [ ] 동일 파일명이 존재하면 경고한다.
- [ ] 권한 오류와 일반 rename 오류를 구분해 표시한다.
- [ ] 성공 후 데이터 key, 현재 경로, 표시 목록을 새 경로로 갱신한다.
- [ ] 성공 Toast를 표시하고 새 행을 다시 선택한다.
- [ ] `다시 불러오기`는 기존 체크 상태를 보존한다.
- [ ] 다시 불러오기 성공/중첩 압축/실패/파일 없음 Toast를 구분한다.

### 5.5 내부 파일 목록과 순서

- [ ] 컬럼은 이전 이름/새 이름/파일 크기/순서 변경이다.
- [ ] 지원 이미지 확장자만 표시한다.
- [ ] 자연 정렬 결과가 원본과 동일하다.
- [ ] 첫 표지 이미지와 일반 이미지 처리 규칙이 동일하다.
- [ ] 드래그 앤 드롭으로 순서를 변경할 수 있다.
- [ ] 위/아래 이동 버튼 또는 원본 순서 조작 UI가 동일하다.
- [ ] 순서 변경 후 새 이름 번호가 즉시 다시 계산된다.
- [ ] 내부 항목 선택 시 해당 이미지를 미리보기로 로드한다.
- [ ] 이미지 로딩 실패 시 placeholder를 표시한다.

### 5.6 분석 예외

- [ ] ZIP/CBZ는 native 분석 후 필요 시 7z fallback을 사용한다.
- [ ] CBR/RAR/7z는 7z로 분석한다.
- [ ] 내부에 또 다른 압축 파일이 있는 경우 nested 목록으로 분리한다.
- [ ] 지원 이미지가 없는 압축은 스킵한다.
- [ ] 암호화/손상 압축은 오류 또는 스킵 로그에 명확히 표시한다.
- [ ] 같은 파일을 다시 추가해도 중복되지 않는다.

### 5.7 실행

- [ ] 체크된 대상이 없으면 경고한다.
- [ ] 실행 전 구조 정리/메타데이터 탭 목록을 원본처럼 비운다.
- [ ] 새 이름 충돌을 2단계 임시 이름 방식으로 회피한다.
- [ ] 폴더 평탄화 ON/OFF를 존중한다.
- [ ] 이미지 최적화는 JPEG/PNG/WebP 도구를 확장자별로 사용한다.
- [ ] WebP 변환은 설정 품질을 사용한다.
- [ ] EXIF 제거 체크가 실제 출력에 반영된다.
- [ ] 출력 포맷을 존중한다.
- [ ] 백업 ON이면 원본을 `bak` 경로에 보존한다.
- [ ] 원본 삭제/유지 정책이 Python과 동일하다.
- [ ] 취소 시 임시 아카이브를 정리하고 원본을 보존한다.
- [ ] 완료 후 목록을 새 파일 경로로 다시 로드한다.
- [ ] 기존 선택 아카이브에 대응하는 새 행을 선택한다.
- [ ] 완료 로그에서 메타데이터 탭 계속 버튼이 동작한다.

---

## 6. 메타데이터 관리 탭

원본 근거: `ui/tabs/tab3_metadata.py`, `ui/api_search_dialog.py`, `ui/tag_widgets.py`, `tasks/save_task.py`, `tasks/api_workers.py`, `core/api_fetcher.py`  
현재 대응: `src/tabs/MetadataTab.jsx`, `electron/tasks/metadataTask.js`, `electron/tasks/saveTask.js`

기준 스크린샷: `old_project/ui_screenshot/4. 메타데이터 관리/1.png` ~ `9.png`

### 6.1 빈 화면과 파일 로드

- [ ] 빈 화면에 drag-and-drop 이미지와 안내 문구를 표시한다.
- [ ] 파일/폴더 추가와 드롭을 지원한다.
- [ ] 폴더에서 지원 아카이브를 재귀 탐색한다.
- [ ] ZIP/CBZ/CBR/RAR/7z를 분석한다.
- [ ] ComicInfo.xml을 대소문자 구분 없이 찾는다.
- [ ] XML이 없으면 빈 메타데이터 기본 구조를 만든다.
- [ ] 첫 이미지 커버를 비동기로 추출한다.
- [ ] 로드 중 진행 상태를 표시하고 UI가 멈추지 않는다.
- [ ] 같은 파일을 중복 추가하지 않는다.

### 6.2 좌측 커버와 파일 트리

- [ ] 좌측 패널 폭은 280px 기준이다.
- [ ] 커버 영역 높이는 350px 기준이다.
- [ ] 폴더별 최상위 그룹과 하위 파일 구조를 표시한다.
- [ ] 그룹 확장/축소 상태를 기억한다.
- [ ] 긴 그룹명은 가로 스크롤로 볼 수 있다.
- [ ] 파일 선택 시 커버와 우측 폼을 갱신한다.
- [ ] 폴더 그룹 선택 시 우측 편집 영역 잠금 규칙이 원본과 같다.
- [ ] Delete 키로 파일/그룹을 목록에서 제거한다.
- [ ] 삭제 후 인접 파일 또는 그룹을 선택한다.
- [ ] 목록이 비면 빈 화면으로 돌아간다.

### 6.3 우측 선택 잠금 overlay

- [ ] 파일 미선택 시 우측 패널에 반투명 overlay가 표시된다.
- [ ] overlay 클릭 시 “왼쪽 리스트에서 작업할 책을 선택” 정보 팝업이 뜬다.
- [ ] 파일 선택 시 overlay가 사라지고 입력이 활성화된다.
- [ ] 저장 중에는 별도 작업 잠금 상태가 적용된다.

### 6.4 API 검색 입력

- [ ] API dropdown 순서는 리디북스/알라딘/Google Books/Anilist/Vine이다.
- [ ] 마지막 선택 API를 config에서 복원한다.
- [ ] API 변경 즉시 config에 저장한다.
- [ ] 검색어 input placeholder가 원본과 같다.
- [ ] Enter로 검색한다.
- [ ] `검색 (S)` 버튼으로 검색한다.
- [ ] 입력 요소에 focus가 없을 때 S 단축키로 검색한다.
- [ ] 검색어가 비면 Toast를 표시하고 요청하지 않는다.
- [ ] 필요한 API 키가 없으면 명확한 경고를 표시한다.

### 6.5 상단 내비게이션과 작업 버튼

- [ ] 기본 정보/작가 및 제작진/출판 정보/장르·태그·등장인물/기타 정보 segmented 버튼이 있다.
- [ ] 클릭 시 해당 폼 구역으로 스크롤한다.
- [ ] 현재 구역 버튼 active 스타일이 스크롤과 동기화된다.
- [ ] `이전 권`, `다음 권`이 현재 그룹 안에서 이동한다.
- [ ] 첫/마지막 항목에서 버튼 상태가 정확하다.
- [ ] `시리즈 초기화`는 확인 팝업 후 현재 시리즈 데이터를 초기화한다.
- [ ] `최신권 정보 불러오기 (D)`가 같은 시리즈의 최신 권 데이터를 가져온다.
- [ ] input focus가 없을 때 D 단축키가 동작한다.
- [ ] `편집창에 원본 복사`가 내 데이터 전체를 일괄 편집창으로 복사한다.
- [ ] `현재 책에 편집 적용`이 일괄 편집 값을 현재 책에 적용한다.
- [ ] `시리즈 전체에 일괄 적용 (C)`가 같은 시리즈 전체에 적용한다.
- [ ] input focus가 없을 때 C 단축키가 동작한다.
- [ ] `빈 값도 덮어쓰기` OFF면 빈 편집값은 원본을 보존한다.
- [ ] ON이면 빈 값도 대상 필드를 비운다.
- [ ] 각 버튼 tooltip이 원본 i18n과 단축키를 표시한다.

### 6.6 메타데이터 필드

- [ ] 모든 ComicInfo 필드가 원본과 동일한 구역/순서로 존재한다.
- [ ] 각 필드에 `내 데이터`와 `일괄 편집/검색 결과` 입력이 짝으로 있다.
- [ ] Series, Title, Number, Count, Volume, AlternateSeries, AlternateNumber, AlternateCount를 지원한다.
- [ ] Summary, Notes, Web을 multiline 입력으로 지원한다.
- [ ] Writer, Penciller, Inker, Colorist, Letterer, CoverArtist, Editor, Translator를 지원한다.
- [ ] Publisher, Imprint, Genre, Tags, Characters, Teams, Locations를 지원한다.
- [ ] PageCount, LanguageISO, Format, BlackAndWhite, Manga, AgeRating, CommunityRating을 지원한다.
- [ ] Year, Month, Day 입력과 날짜 유효성 처리가 동일하다.
- [ ] 숫자 필드는 빈 값과 0을 원본과 동일하게 구분한다.
- [ ] +/- 보조 버튼이 있는 숫자 필드는 0 아래로 내려가지 않는다.
- [ ] dropdown의 빈 항목, 표시 문구, 실제 저장 값이 정확히 매핑된다.
- [ ] searchable combo는 사용자 입력과 기존 옵션 선택을 모두 지원한다.
- [ ] 태그 입력은 Enter 또는 구분자로 chip을 생성한다.
- [ ] 태그 중복을 제거한다.
- [ ] chip의 × 버튼으로 개별 삭제한다.
- [ ] IME 조합 중 Enter가 태그를 조기에 확정하지 않는다.
- [ ] 설정의 태그 표준화 규칙을 API/수동 입력에 적용한다.
- [ ] 필드별 복사 버튼이 일괄 편집 값을 내 데이터로 복사한다.
- [ ] 긴 텍스트와 다국어 문자가 잘리지 않는다.

### 6.7 자동 입력

- [ ] `시리즈 자동 매칭`이 현재 제목/시리즈로 API 검색 후 시리즈 전체에 적용한다.
- [ ] 파일명에서 시리즈와 제목을 추론하는 결과가 Python parser와 같다.
- [ ] `제목 자동 입력`이 권/화 표기와 언어별 형식을 반영한다.
- [ ] `권수 자동 입력`이 `Vol`, `V`, `권`, 끝 숫자를 인식한다.
- [ ] `화수 자동 입력`이 `Ch`, `Chapter`, `화`를 인식한다.
- [ ] `페이지 자동 입력`이 압축 내부 지원 이미지 개수를 센다.
- [ ] 각 자동 입력 완료 시 해당 Toast를 표시한다.
- [ ] 자동 입력은 현재 시리즈 범위에만 적용된다.

### 6.8 API 검색 결과 다이얼로그

- [ ] 헤더에 현재 작품명이 큰 제목으로 표시된다.
- [ ] API dropdown과 검색어 input, 검색 버튼이 있다.
- [ ] API 키 누락 경고 label이 조건부 표시된다.
- [ ] Enter와 S 단축키로 재검색한다.
- [ ] 입력 focus 중 S/C 단축키는 가로채지 않는다.
- [ ] 좌측 결과 목록에 썸네일과 핵심 정보가 표시된다.
- [ ] 결과 선택 시 우측 상세가 갱신된다.
- [ ] 이전/다음 페이지 버튼과 현재 페이지가 표시된다.
- [ ] 첫 페이지에서 이전 버튼을 비활성화한다.
- [ ] 다음 결과가 없으면 다음 버튼을 비활성화한다.
- [ ] 결과 건수를 표시한다.
- [ ] 우측에 제목, 커버, 작가, 출판사, 장르, 전체 권수, 평점, 연령등급, 출간일을 표시한다.
- [ ] 줄거리, 태그, 링크를 표시한다.
- [ ] 링크는 클릭해 외부 브라우저로 열 수 있다.
- [ ] 텍스트는 마우스로 선택/복사할 수 있다.
- [ ] 검색 중 loading 상태와 이전 결과 제거가 명확하다.
- [ ] 결과가 없을 때 빈 상태를 표시한다.
- [ ] 검색 결과는 원본과 동일한 기간 동안 캐시된다.
- [ ] 리디북스 출간일 추가 조회가 선택 결과에 반영된다.
- [ ] 번역 가능한 결과에 번역 버튼을 표시한다.
- [ ] 번역 성공 시 원문/번역문 전환이 가능하다.
- [ ] 번역 실패 시 오류를 표시하고 원문을 유지한다.
- [ ] `닫기`는 결과를 적용하지 않는다.
- [ ] `선택 (C)` 또는 C 단축키는 선택 결과를 편집창에 적용한다.
- [ ] 선택 항목이 없으면 적용하지 않는다.
- [ ] Manga 값이 없으면 `YesAndRightToLeft` 기본값을 적용한다.
- [ ] Summary의 `<책소개>`와 과도한 빈 줄을 정리한다.

### 6.9 저장

- [ ] 단일 저장 버튼과 전체 저장 버튼의 색상/위치가 동일하다.
- [ ] Ctrl/Cmd+S로 단일 저장한다.
- [ ] Ctrl/Cmd+Shift+S로 전체 저장한다.
- [ ] 저장 전 현재 UI 값을 내부 데이터에 반영한다.
- [ ] 단일 저장 전 다른 작업 탭 목록을 원본처럼 비운다.
- [ ] 전체 저장 전 다른 작업 탭 목록을 원본처럼 비운다.
- [ ] 저장 중 파일 트리와 우측 폼을 잠근다.
- [ ] 상태에 사용 엔진(7z/WinRAR)을 표시한다.
- [ ] ZIP/CBZ에 ComicInfo.xml이 없으면 append한다.
- [ ] 기존 XML이 있으면 안전하게 갱신한다.
- [ ] 7z는 7z 명령으로 갱신한다.
- [ ] RAR/CBR은 Windows에서 WinRAR 필요 조건을 안내한다.
- [ ] macOS에서 지원 불가능한 RAR 갱신은 명확히 안내한다.
- [ ] XML에 namespace를 포함한다.
- [ ] 빈 필드는 XML에서 원본과 동일하게 제외한다.
- [ ] `ComicZipAddedDate`는 기존 값이 있으면 보존한다.
- [ ] `ComicZipModifiedDate`는 저장 시각으로 갱신한다.
- [ ] 전체 저장은 max_threads 설정을 존중한다.
- [ ] 단일 성공 시 Toast, 완료음, 트리 새로고침을 수행한다.
- [ ] 단일 실패 시 사유를 경고하고 편집 영역을 다시 활성화한다.
- [ ] 전체 완료 시 성공/실패 개수 Toast를 표시한다.
- [ ] 일부 성공이면 완료음을 재생한다.
- [ ] 취소/앱 종료 시 아카이브 손상을 방지한다.

---

## 7. 환경설정

원본 근거: `ui/dialogs.py::SettingsDialog`, `config.py`  
현재 대응: `src/components/SettingsModal.jsx`, `electron/configManager.js`

기준 스크린샷: `old_project/ui_screenshot/환경 설정/1.png` ~ `3.png`

### 7.1 모달 공통

- [ ] 제목은 `환경 설정` i18n을 사용한다.
- [ ] 기준 크기 500×750, 최소 크기 500×750을 재현한다.
- [ ] 탭은 기본 설정/폴더 탭 설정/API 검색 설정 순서다.
- [ ] 저장/취소 버튼의 문구, 순서, 색상이 원본과 같다.
- [ ] Escape 또는 취소는 값을 저장하지 않는다.
- [ ] 저장은 모든 입력을 정규화한 뒤 config에 반영한다.
- [ ] 모달 내 스크롤 시 하단 저장/취소 접근성이 유지된다.

### 7.2 기본 설정

- [ ] 언어 dropdown은 한국어/English/日本語 순서다.
- [ ] 현재 `lang` 값을 표시 문구에 정확히 매핑한다.
- [ ] 완료 알림 소리 dropdown은 `Default.wav`를 첫 항목으로 표시한다.
- [ ] sounds 폴더의 mp3/wav를 이름순으로 표시한다.
- [ ] 소리 선택 시 즉시 미리듣기한다.
- [ ] 소리 파일이 없거나 재생 실패해도 설정 모달이 깨지지 않는다.
- [ ] 폰트 dropdown에 Default와 설치/지원 폰트를 표시한다.
- [ ] 폰트 배율 dropdown은 80%~155%, 5% 단위다.
- [ ] 출력 포맷 dropdown은 변경 안 함/ZIP/CBZ/CBR/7z 순서다.
- [ ] 스레드 slider의 최소/최대가 CPU 코어 기준 원본과 같다.
- [ ] 현재 스레드 수를 숫자로 표시한다.
- [ ] 완료음 재생 체크를 저장한다.
- [ ] 백업 체크를 저장한다.
- [ ] 폴더 평탄화 체크를 저장한다.
- [ ] 스킵 파일도 메타데이터 관리로 넘기기 체크와 tooltip이 있다.
- [ ] 이미지 품질 slider와 현재 값 표시가 있다.
- [ ] WebP 변환 체크가 있다.
- [ ] WebP 관련 품질/옵션 활성 상태가 체크와 연동된다.
- [ ] 외부 뷰어 경로 input과 `찾기` 버튼이 있다.
- [ ] 뷰어 선택 취소 시 기존 경로를 유지한다.

### 7.3 폴더 탭 설정

- [ ] 중복 검사 대상/라이브러리 폴더 목록을 표시한다.
- [ ] `추가`가 폴더 선택기를 연다.
- [ ] 같은 폴더를 중복 추가하지 않는다.
- [ ] `삭제`가 선택한 목록 항목만 제거한다.
- [ ] 선택이 없을 때 삭제해도 오류가 나지 않는다.
- [ ] `인덱스 색인 갱신` 버튼과 설명이 있다.
- [ ] 갱신 중 버튼 중복 실행을 막고 진행 결과를 표시한다.
- [ ] `중복 매칭 캐시 초기화` 버튼과 설명이 있다.
- [ ] 캐시 삭제 전 확인 질문이 원본과 동일하다.
- [ ] 취소 시 캐시를 지우지 않는다.
- [ ] 완료 시 삭제 건수 또는 완료 Toast를 표시한다.

### 7.4 API 검색 설정

- [ ] AI 공식 영문명 변환 사용 체크가 있다.
- [ ] AI provider dropdown은 Gemini/OpenAI 순서다.
- [ ] AI 체크 OFF 시 provider와 API key를 비활성화한다.
- [ ] AI 체크 ON 시 다시 활성화한다.
- [ ] AI key는 password input이다.
- [ ] 눈 아이콘 클릭으로 각 secret input을 개별 표시/숨김한다.
- [ ] 표시 상태에 따라 eye/eye-slash 아이콘과 active 스타일이 바뀐다.
- [ ] Aladin TTBKey input이 있다.
- [ ] Google Books API key input이 있다.
- [ ] Comic Vine API key input이 있다.
- [ ] `API 발급 매뉴얼` 버튼이 원본 안내 다이얼로그/링크를 연다.
- [ ] 태그 표준화 규칙 textarea가 있다.
- [ ] placeholder 예시는 `기존태그 -> 새태그` 형식을 보여준다.
- [ ] 여러 source 태그와 쉼표 구분 규칙을 동일하게 파싱한다.
- [ ] `검색 캐시 비우기` 버튼이 API 결과와 커버 캐시를 삭제한다.
- [ ] 캐시 삭제 전 확인 질문이 있다.
- [ ] 완료 시 Toast를 표시한다.

### 7.5 설정 적용 부수 효과

- [ ] 출력 포맷/WebP/이미지 품질 변경 시 작업 탭 목록을 비운다.
- [ ] 목록 초기화 사실을 Toast로 알린다.
- [ ] 언어/폰트/배율 변경 시 재시작 질문을 표시한다.
- [ ] 재시작 질문에서 `예`는 창 상태 저장 후 앱을 재시작한다.
- [ ] `아니오`는 재시작 없이 가능한 문구를 즉시 갱신한다.
- [ ] 라이브러리 폴더 변경 시 폴더 탭으로 이동하고 인덱싱한다.
- [ ] 설정 저장 후 즐겨찾기/라이브러리 빠른 목록을 갱신한다.
- [ ] 내부 파일명 변경 미리보기를 새 설정으로 갱신한다.
- [ ] 기존 Python config key와 새 key의 양방향 호환을 유지한다.

---

## 8. 공유 서버

원본 근거: `ui/tabs/tab_sharing.py`, `servers/manager.py`, `servers/opds_server.py`, `servers/webdav_server.py`  
현재 대응: `src/tabs/SharingTab.jsx`, `electron/servers/sharingServers.js`

기준 스크린샷: `old_project/ui_screenshot/5. 공유 서버/1.png`, `2.png`

### 8.1 OPDS

- [ ] 그룹 제목에 Panels 지원 문구가 표시된다.
- [ ] 포트 입력 범위는 1024~65535다.
- [ ] 기본 포트는 8080이다.
- [ ] 포트 변경 즉시 config와 URL을 갱신한다.
- [ ] 서버 OFF 시 `OPDS 서버 켜기`와 power 아이콘을 표시한다.
- [ ] 서버 ON 시 `OPDS 서버 끄기`와 stop 아이콘을 표시한다.
- [ ] 서버 시작 중 중복 클릭을 막는다.
- [ ] 시작 성공 시 포트 input을 비활성화한다.
- [ ] 중지 성공 시 포트 input을 다시 활성화한다.
- [ ] URL은 `http(s)://로컬IP:포트/opds` 형식이다.
- [ ] URL input은 읽기 전용이다.
- [ ] URL 복사 버튼이 정확한 URL을 clipboard에 넣는다.
- [ ] OPDS root에 라이브러리 목록이 표시된다.
- [ ] 하위 폴더 탐색이 가능하다.
- [ ] 아카이브 다운로드 MIME과 파일명이 클라이언트에서 정상 인식된다.
- [ ] Panels 실제 기기/앱에서 목록 탐색과 다운로드를 검증한다.

### 8.2 WebDAV

- [ ] 그룹 제목에 ComicGlass 지원 문구가 표시된다.
- [ ] 아이디 기본값은 `user`다.
- [ ] 비밀번호 기본값은 `1234`다.
- [ ] 빈 아이디/비밀번호 저장 시 기본값 보정이 원본과 같다.
- [ ] 비밀번호는 기본 마스킹된다.
- [ ] eye 아이콘으로 표시/숨김을 토글한다.
- [ ] 포트 범위는 1024~65535, 기본값은 8081이다.
- [ ] 서버 ON 시 포트/아이디/비밀번호 입력을 비활성화한다.
- [ ] 서버 OFF 시 다시 활성화한다.
- [ ] URL은 `http://로컬IP:포트/` 형식이다.
- [ ] URL 복사가 동작한다.
- [ ] Basic Auth 실패 시 401과 인증 헤더를 반환한다.
- [ ] PROPFIND depth 처리와 XML 응답이 ComicGlass에서 인식된다.
- [ ] 폴더 탐색과 파일 다운로드가 가능하다.
- [ ] 등록 라이브러리 밖 경로 접근을 차단한다.
- [ ] path traversal과 잘못된 URL encoding을 차단한다.
- [ ] ComicGlass 실제 기기/앱에서 로그인, 탐색, 다운로드를 검증한다.

### 8.3 상태 로그와 전역 상태

- [ ] 오른쪽 로그 패널이 원본 레이아웃과 동일하다.
- [ ] 일반 메시지는 `[INFO]`로 표시한다.
- [ ] 오류는 `[ERROR]`와 빨간색으로 표시한다.
- [ ] 서버 시작/중지/포트 충돌/인증 오류를 로그에 남긴다.
- [ ] 새 로그가 추가되면 마지막 줄이 보이도록 스크롤한다.
- [ ] 하나 이상의 서버 실행 시 메인 하단 서버 상태 아이콘을 표시한다.
- [ ] 앱 종료 시 실행 중인 서버를 안전하게 닫는다.
- [ ] 포트 충돌 시 버튼/입력 상태가 OFF 상태로 복구된다.
- [ ] 네트워크 인터페이스가 없으면 127.0.0.1 fallback을 표시한다.

---

## 9. 업데이트 및 릴리즈 노트

원본 근거: `ui/main_window.py`, `tasks/update_task.py`  
현재 대응: `src/tabs/ReleaseTab.jsx`, `electron/ipcHandlers.js`

기준 스크린샷: `old_project/ui_screenshot/6. 업데이트 및 릴리즈 노트/1.png`

- [ ] 앱 시작 후 현재 버전과 최신 버전을 비동기로 비교한다.
- [ ] 네트워크 실패가 앱 시작을 막지 않는다.
- [ ] 최신 버전이 없으면 현재 버전 버튼 스타일을 유지한다.
- [ ] 업데이트가 있으면 버전 버튼을 녹색 update 스타일로 바꾼다.
- [ ] 버전 버튼 클릭 시 업데이트 여부 확인 또는 릴리즈 URL을 연다.
- [ ] 릴리즈 노트를 최신순으로 표시한다.
- [ ] 버전, 날짜, 제목, 본문 형식이 원본과 같다.
- [ ] Markdown/링크가 안전하게 렌더링된다.
- [ ] 로딩 중 상태가 표시된다.
- [ ] 네트워크 실패 시 fallback 문구를 표시한다.
- [ ] 외부 링크는 기본 브라우저로 연다.
- [ ] 자동 업데이트 스크립트를 유지한다면 다운로드, 검증, 재실행, 실패 복구를 검증한다.

---

## 10. 다국어, 폰트, 아이콘, 리소스

원본 근거: `core/i18n.py`, `fonts/`, `sounds/`, `src/`  
현재 대응: `src/utils/i18nData.js`, `src/hooks/useI18n.js`, `src/images/`, 전역 CSS

### 10.1 다국어

- [ ] Python i18n의 모든 key를 Electron i18n에 옮겼다.
- [ ] 한국어/영어/일본어에서 누락 key가 화면에 key 문자열로 노출되지 않는다.
- [ ] fallback 언어와 fallback 문구가 원본과 같다.
- [ ] `{count}`, `{msg}`, positional format 값이 정확히 치환된다.
- [ ] 탭, 버튼, tooltip, placeholder, popup, Toast, 로그까지 모두 번역된다.
- [ ] 언어별 권/화/Vol/Ch/巻/話 표기가 parser와 출력 파일명에 반영된다.
- [ ] 한국어/일본어 IME 조합이 input, 검색, 태그, 이름 변경에서 안전하다.

### 10.2 폰트와 배율

- [ ] NotoSansKR-Regular.ttf를 번들하고 로드한다.
- [ ] Jua-Regular.ttf를 번들하고 로드한다.
- [ ] Default 선택 시 플랫폼 기본 폰트 fallback이 안정적이다.
- [ ] font scale이 모든 `s11/s12/s13/s14/s16/s20` 대응 요소에 일관되게 적용된다.
- [ ] 배율 80%, 100%, 155%에서 버튼과 입력이 잘리지 않는다.
- [ ] 표 행 높이와 아이콘 정렬이 폰트 배율에 맞게 증가한다.

### 10.3 이미지, 아이콘, 사운드

- [ ] draganddrop1/2/3 이미지를 그대로 재사용한다.
- [ ] nodata/nodata2와 rainbow cat 리소스 사용 위치를 원본과 대조한다.
- [ ] 모든 Font Awesome 아이콘의 종류와 의미가 원본과 같다.
- [ ] 아이콘 색상이 normal/hover/active/disabled 상태에 맞다.
- [ ] sounds 폴더의 모든 mp3/wav를 빌드에 포함한다.
- [ ] 선택 완료음이 개발/패키지 모드 모두에서 재생된다.
- [ ] 완료음 OFF 시 어떤 작업도 소리를 재생하지 않는다.

---

## 11. 압축·이미지·파일 시스템 로직

원본 근거: `core/archive_utils.py`, `core/parser.py`, `tasks/*.py`, `bin/win/`  
현재 대응: `electron/core/`, `electron/tasks/`, `electron/parsers/`, `bin/win/`

### 11.1 지원 형식

- [ ] ZIP 읽기/쓰기
- [ ] CBZ 읽기/쓰기
- [ ] 7z 읽기/쓰기
- [ ] RAR 읽기
- [ ] CBR 읽기
- [ ] RAR/CBR 메타데이터 쓰기의 플랫폼 제한 안내
- [ ] 대문자 확장자 처리
- [ ] 확장자와 실제 포맷 불일치 처리
- [ ] 암호화 압축 처리
- [ ] 손상 압축 처리
- [ ] 빈 압축 처리
- [ ] 내부 한글/일본어 파일명 인코딩 처리

### 11.2 외부 바이너리

- [ ] Windows 패키지에서 7za.exe 경로를 찾는다.
- [ ] macOS에서 bundled/system 7z fallback을 찾는다.
- [ ] cwebp, pngquant, jpegtran 실행 경로를 찾는다.
- [ ] Windows에서 subprocess 창이 뜨지 않는다.
- [ ] 인자에 공백/한글/특수문자 경로를 안전하게 전달한다.
- [ ] exit code와 stderr를 오류 로그에 반영한다.
- [ ] 실행 파일이 없을 때 사용자에게 필요한 조치를 안내한다.
- [ ] 취소 시 child process를 안전하게 종료한다.

### 11.3 임시 파일과 원자성

- [ ] 임시 디렉터리 이름 충돌을 방지한다.
- [ ] 출력 완성 전 원본을 삭제하지 않는다.
- [ ] 같은 경로 교체는 임시 아카이브 완성 후 원자적으로 수행한다.
- [ ] 실패 시 원본을 복구한다.
- [ ] backup 설정 ON/OFF를 모든 작업에서 일관되게 적용한다.
- [ ] 성공/실패/취소 후 temp 파일을 정리한다.
- [ ] 앱 강제 종료 후 남은 temp가 다음 실행을 방해하지 않는다.

### 11.4 parser 동등성

- [ ] 시리즈명 추출 결과를 대표 파일명 corpus로 Python과 비교한다.
- [ ] 제목 추출 결과를 비교한다.
- [ ] 권수 추출 결과를 비교한다.
- [ ] 화수 추출 결과를 비교한다.
- [ ] 시즌/부/part/외전 인식 결과를 비교한다.
- [ ] 소수 권수, 범위, 합본 표기를 비교한다.
- [ ] 괄호, 대괄호, 작가명, 릴리즈 그룹 제거 결과를 비교한다.
- [ ] 한국어/영어/일본어 표기 결과를 비교한다.

---

## 12. 설정, DB, 캐시 호환성

원본 근거: `config.py`, `core/library_db.py`, `.api_cache.db`, `data/library.db`  
현재 대응: `electron/configManager.js`, `electron/database/library_db.js`

- [ ] 기존 config.json을 Electron에서 손실 없이 읽는다.
- [ ] 알 수 없는 기존 key를 저장 시 삭제하지 않는다.
- [ ] `lang`과 `language` 호환을 유지한다.
- [ ] `target_format`, `backup_on`, `flatten_folders`, `webp_conversion`, `img_quality`, `max_threads`, `play_sound`, `completion_sound`를 유지한다.
- [ ] `dup_check_folders`, `libraries`, `favorites`의 중복과 형식을 정규화한다.
- [ ] `api_keys` 내부 key를 유지한다.
- [ ] 창, 탭, splitter, 보기 모드, 정렬, 그룹, 레이아웃 상태를 저장한다.
- [ ] config 파일이 없으면 원본 기본값으로 생성한다.
- [ ] config JSON이 손상되면 백업하고 안전한 기본값으로 복구한다.
- [ ] 여러 빠른 변경이 config write race를 만들지 않는다.
- [ ] library.db schema와 기존 데이터 마이그레이션을 검증한다.
- [ ] 중복 인덱스 갱신이 신규/수정/삭제 파일을 반영한다.
- [ ] API cache TTL이 원본과 같다.
- [ ] 캐시 삭제가 DB row와 썸네일 파일을 모두 정리한다.

---

## 13. 접근성 및 세밀한 상호작용

- [ ] 모든 클릭 가능한 요소에 pointer cursor가 있다.
- [ ] disabled 요소는 색상뿐 아니라 실제 입력도 차단한다.
- [ ] Tab 키 focus 순서가 시각적 순서와 같다.
- [ ] focus ring이 어두운 배경에서 보인다.
- [ ] Enter가 multiline input에서 의도치 않게 저장/검색을 실행하지 않는다.
- [ ] Escape가 최상위 모달부터 닫는다.
- [ ] 모달 뒤 페이지가 스크롤되지 않는다.
- [ ] 우클릭 메뉴가 바깥 클릭/Escape/항목 선택 후 닫힌다.
- [ ] dropdown이 화면 아래에서 잘리면 위쪽으로 열린다.
- [ ] tooltip은 hover 후 표시되고 클릭을 막지 않는다.
- [ ] 버튼 double-click이 작업을 두 번 시작하지 않는다.
- [ ] 느린 IPC 응답 중 로딩/disabled 상태를 표시한다.
- [ ] renderer unmount 후 늦은 IPC event가 state를 변경하지 않는다.
- [ ] 긴 작업 중 Electron 창이 “응답 없음” 상태가 되지 않는다.
- [ ] 키보드 단축키는 input/textarea/select focus 중 텍스트 입력을 침범하지 않는다.
- [ ] Windows의 Ctrl과 macOS의 Cmd를 저장/전체 선택 등에 올바르게 매핑한다.

---

## 14. 알림·질문·input·dropdown 전수 검증표

이 절은 “작은 기능” 누락 방지를 위한 최종 전수 검사다.

### 14.1 질문/확인 팝업

- [ ] 업데이트 페이지 이동/업데이트 실행 확인
- [ ] 설정 변경 후 앱 재시작 확인
- [ ] 메타데이터 탭 파일 드롭 시 부모 폴더 전체 추가 여부
- [ ] 작업 중 앱 종료 확인
- [ ] 폴더/파일 실제 삭제 확인
- [ ] 시리즈 메타데이터 초기화 확인
- [ ] 시리즈 그룹 삭제 확인
- [ ] API 검색 캐시 삭제 확인
- [ ] 중복 매칭 캐시 삭제 확인
- [ ] 폴더 인덱스 스마트 업데이트/강제 재스캔/취소 선택
- [ ] 구조 정리/이름 변경 결과 후 다음 탭 계속 여부

### 14.2 정보/경고/오류

- [ ] 실행 대상 없음
- [ ] 추가 파일 중 nested archive 존재
- [ ] 지원하지 않는 파일 존재
- [ ] 파일이 존재하지 않음
- [ ] 동일 이름 존재
- [ ] 파일 rename 권한 오류
- [ ] 일반 rename 오류
- [ ] 외부 뷰어 미설정
- [ ] 잘못된 이동 경로
- [ ] CSV 데이터 없음/완료/실패
- [ ] 실행 취소 기록 없음/실패
- [ ] 누락 권수 없음
- [ ] 등록 라이브러리 없음
- [ ] 메타데이터 저장 실패
- [ ] RAR/CBR 저장에 WinRAR 필요
- [ ] 서버 포트 충돌/시작 실패/중지 실패
- [ ] API key 누락/검색 실패/번역 실패
- [ ] 작업 취소 완료

### 14.3 input 전수

- [ ] 폴더 검색
- [ ] 폴더/파일 단일 이름 변경
- [ ] 여러 파일 이름 변경 기존/새 형식
- [ ] 레이아웃 이름 저장
- [ ] 삭제할 레이아웃 선택
- [ ] 경로로 이동
- [ ] 구조 정리 출력 경로
- [ ] 구조 정리 자식 파일명
- [ ] 내부 파일명 custom pattern
- [ ] 내부 파일명 시작 번호
- [ ] 메타데이터 검색어
- [ ] 모든 ComicInfo text/number/date/multiline/tag 입력
- [ ] API 검색 다이얼로그 검색어
- [ ] 뷰어 경로
- [ ] WebDAV 아이디/비밀번호
- [ ] API secret key
- [ ] 태그 치환 규칙 textarea

### 14.4 dropdown 전수

- [ ] 환경설정 언어
- [ ] 완료 알림 소리
- [ ] 폰트
- [ ] 폰트 배율
- [ ] 출력 포맷
- [ ] AI provider
- [ ] 폴더 그룹 기준
- [ ] 폴더 필터 checkable action
- [ ] 폴더 정렬 기준/순서
- [ ] 저장 레이아웃 적용/삭제
- [ ] 라이브러리 이동 대상
- [ ] 여러 파일 이름 변경 순번 위치
- [ ] 내부 파일명 패턴
- [ ] 메타데이터 검색 API
- [ ] 메타데이터 Format/Manga/Yes-No/등급/언어 등 모든 combo
- [ ] API 검색 다이얼로그 API

---

## 15. 테스트 시나리오

### 15.1 최소 샘플 세트

- [ ] 정상 ZIP 1권
- [ ] 정상 CBZ 1권
- [ ] 다중 폴더 ZIP
- [ ] 압축 안의 압축
- [ ] ComicInfo.xml 포함 파일
- [ ] ComicInfo.xml 미포함 파일
- [ ] 커버 없는 파일
- [ ] 한글/영문/일본어 파일명
- [ ] 같은 이름 파일 2개
- [ ] 손상 ZIP
- [ ] 암호화 ZIP
- [ ] RAR/CBR
- [ ] 7z
- [ ] 이미지 1장/9장/10장/99장/100장 파일
- [ ] NAS 또는 네트워크 경로 파일
- [ ] 읽기 전용/권한 없는 경로 파일

### 15.2 회귀 흐름

- [ ] 폴더 탭에서 파일 선택 → 구조 정리 탭 전달 → 실행 → 이름 변경 탭 계속
- [ ] 이름 변경 실행 → 메타데이터 탭 계속 → API 검색 → 전체 저장
- [ ] 폴더 탭에서 여러 파일 이름 변경 → 실행 취소
- [ ] 폴더 탭에서 시리즈별 폴더 정리 → 라이브러리 이동 → 이름 충돌 해결
- [ ] 설정에서 포맷/WebP 변경 → 목록 초기화 알림 → 새 설정으로 재분석
- [ ] 언어/폰트 변경 → 재시작 → 창/탭/설정 복원
- [ ] OPDS/WebDAV 동시 실행 → 앱 종료 → 포트 정상 해제
- [ ] 긴 작업 시작 → 취소 → 즉시 다른 작업 실행
- [ ] 긴 작업 시작 → 앱 종료 거부 → 작업 계속
- [ ] 긴 작업 시작 → 강제 종료 승인 → 원본 파일 무결성 검사

### 15.3 시각 회귀

- [ ] 기준 스크린샷과 동일 창 크기로 캡처한다.
- [ ] 폴더 탭 8개 화면을 각각 비교한다.
- [ ] 구조 정리 탭 5개 화면을 각각 비교한다.
- [ ] 내부 이름 변경 탭 4개 화면을 각각 비교한다.
- [ ] 메타데이터 탭 9개 화면을 각각 비교한다.
- [ ] 공유 서버 탭 2개 화면을 각각 비교한다.
- [ ] 릴리즈 탭 1개 화면을 비교한다.
- [ ] 환경설정 3개 탭을 각각 비교한다.
- [ ] 색상, 여백, 폭, 높이, 줄바꿈, scrollbar, hover, focus 차이를 기록한다.
- [ ] 100% 배율과 125% 이상 배율에서 다시 비교한다.

---

## 16. 현재 구현 파일별 감사 대상

아래 파일은 “구현됨” 표시가 아니라 원본과 대조해야 할 현재 코드 위치다.

- [ ] `src/App.jsx` — 공통 툴바, 탭, 상태, 설정, Toast
- [ ] `src/components/SettingsModal.jsx` — 환경설정 전체
- [ ] `src/components/Toast.jsx` — Toast 수명/위치/중복
- [ ] `src/tabs/FolderTab.jsx` — 폴더 탭 조합과 컨텍스트 메뉴
- [ ] `src/components/folder/FolderSidebar.jsx` — 라이브러리/즐겨찾기/트리
- [ ] `src/components/folder/FolderToolbar.jsx` — 그룹/필터/정렬/검색
- [ ] `src/components/folder/FileTableView.jsx` — 상세 보기
- [ ] `src/components/folder/ThumbnailView.jsx` — 썸네일 보기
- [ ] `src/components/folder/TileView.jsx` — 타일 보기
- [ ] `src/components/folder/DetailPanel.jsx` — 상세 정보
- [ ] `src/components/folder/MissingVolumesDialog.jsx` — 누락 권수
- [ ] `src/tabs/OrganizerTab.jsx` — 구조 정리 UI
- [ ] `src/tabs/RenamerTab.jsx` — 내부 파일명 UI
- [ ] `src/tabs/MetadataTab.jsx` — 메타 입력/API 검색/저장
- [ ] `src/tabs/SharingTab.jsx` — OPDS/WebDAV UI
- [ ] `src/tabs/ReleaseTab.jsx` — 릴리즈 노트
- [ ] `electron/ipcHandlers.js` — renderer/main 경계의 모든 입력 검증
- [ ] `electron/configManager.js` — 설정 호환과 원자적 저장
- [ ] `electron/tasks/folderScanTask.js` — 폴더/메타/커버 스캔
- [ ] `electron/tasks/organizerTask.js` — 구조 분석
- [ ] `electron/tasks/organizeTask.js` — 구조 정리 실행
- [ ] `electron/tasks/renamerTask.js` — 내부 이름 분석/실행
- [ ] `electron/tasks/metadataTask.js` — 메타 분석/API 처리
- [ ] `electron/tasks/saveTask.js` — ComicInfo 저장
- [ ] `electron/servers/sharingServers.js` — OPDS/WebDAV 호환성
- [ ] `electron/database/library_db.js` — 인덱스/중복/시리즈 그룹

---

## 17. 최종 출시 게이트

- [ ] 이 문서의 모든 필수 항목이 `[x]`다.
- [ ] 미완료 항목은 명시적인 제외 사유와 사용자 승인을 기록했다.
- [ ] Python과 Electron의 대표 입력 corpus 결과가 일치한다.
- [ ] 기존 config와 library DB를 복사해 실행하는 업그레이드 테스트를 통과했다.
- [ ] Windows portable 빌드에서 외부 바이너리와 리소스를 찾는다.
- [ ] macOS 빌드에서 7z fallback, 권한, Finder 열기를 검증했다.
- [ ] 패키지 설치 경로에 한글/공백이 있어도 동작한다.
- [ ] 모든 작업의 성공/실패/취소 후 원본 파일 hash 또는 무결성을 확인했다.
- [ ] OPDS Panels 및 WebDAV ComicGlass 실제 호환 테스트를 통과했다.
- [ ] 기준 스크린샷 대비 치명적 시각 차이가 없다.
- [ ] console에 반복 오류, unhandled rejection, listener leak 경고가 없다.
- [ ] 신규 설치와 기존 사용자 업그레이드 모두 테스트했다.
